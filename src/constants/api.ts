export enum API_Method {
  GET = "get",
  POST = "post",
  PUT = "put",
  PATCH = "patch",
  DELETE = "delete"
}

export enum API_URL {
  LOGIN = "auth/login",
  POSTS = "posts",
  EMPLOYEES = "employees",
}